# TODO

- [x] Create admin-only dashboard page (`admin/dashboard.php`) with admin-specific content and links to existing admin-like pages.
- [x] Update navigation (`includes/nav.php`) to show Admin Dashboard link only to admins.
- [x] Fix GitHub OAuth connect flow (harden `auth/github_callback.php`, improve debug + error handling).
- [ ] Run quick PHP lint (`php -l`) on modified files (env may not have `php` on PATH; use your server’s php if needed).


